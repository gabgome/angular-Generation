export const environment = {
  production: true,
  token: ''
};

/*O Angular está exportando uma constante chamada 'environment' do tipo objeto que recebe como atributo 'production' e 'token' do tipo string, 
que é referenciado com aspas duplas ou simples. Através do 'environment é possível criar variáveis globais*/
